# from fastapi import APIRouter
# from app.core.singleton import bus_service

# router = APIRouter()

# @router.get("/stop-predictions/{stop_id}")
# async def get_stop_predictions(stop_id: str):
#     return await bus_service.get_stop_predictions(stop_id)
