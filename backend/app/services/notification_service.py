def send_notification(user_id, message):
    """Send a push notification."""
    print(f"Sending notification to {user_id}: {message}")

    # Placeholder: Simulate notification sending
    return {
        "user_id": user_id,
        "message": message,
        "status": "Notification sent successfully"
    }
